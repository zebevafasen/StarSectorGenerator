export const HEX_SIZE = 25;
export const HEX_HEIGHT = Math.sqrt(3) * HEX_SIZE;
export const SIDEBAR_WIDTH = 384;

// Galaxy View Constants
export const SECTOR_PADDING = 0; // Space between stitched sectors (0 for seamless)
export const GALAXY_CHUNK_BUFFER = 1; // How many extra sectors to render off-screen
